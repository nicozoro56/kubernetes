apiVersion: v1
kind: Service
metadata:
  labels:
    app: myservice
  name: myservice
spec:
  ports:
    - nodePort: 30945
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: myservice
  type: LoadBalancer
