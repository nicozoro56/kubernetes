apiVersion: v1
kind: Service
metadata:
  name: myservice
spec:
  ports:
    - nodePort: 31280
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: myservice
  type: NodePort
